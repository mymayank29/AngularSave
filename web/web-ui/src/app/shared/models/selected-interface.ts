export interface Selected {
    wellname: string,
    parent_vendor: string,
    submit_date: string,
    status: string,
    jobtyp: string
}

export interface FilterObject {
    filterName: string;
    filterData: string;
}
