export interface NPTDetail {
    id: string,
    npt_event_no: string,
    typ: string,
    npt_date_start: string,
    npt_date_end: string,
    duration: number,
    npt_type_detail_description: string,
    com: string
}
